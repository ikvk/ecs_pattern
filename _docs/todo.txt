search for places to optimize

удаление большой пачки сущностей за раз приведет к задержке, подумать над оптимизацией на стороне либы
    ...пробовать удалять раз в интервал, или пачку?

multiproc phys example

storing components as
dict(Component: [comp1, comp2])
and using weakref_slot in dataclasses?